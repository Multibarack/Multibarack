apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "platform-api.fullname" . }}-quota
  labels:
    {{- include "platform-api.labels" . | nindent 4 }}
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "20"
