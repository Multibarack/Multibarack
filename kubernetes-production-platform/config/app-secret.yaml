apiVersion: v1
kind: Secret
metadata:
  name: platform-api-secret
  namespace: platform
type: Opaque
stringData:
  APP_NAME: "platform-api"
  # Demo value only. Never commit real credentials.
  APP_MODE: "demo"
