apiVersion: v1
kind: LimitRange
metadata:
  name: platform-defaults
  namespace: platform
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 256Mi
      defaultRequest:
        cpu: 100m
        memory: 64Mi
